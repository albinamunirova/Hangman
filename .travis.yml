language: python
python:
  - 3.5
install:
  - pip3 install codecov
  - pip3 install pytest-cov
script:
  - ./setup.py test
after_success:
  - codecov
before_install:
  - chmod a+x setup.py
